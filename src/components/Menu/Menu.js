import React from 'react'
import Button from '@mui/material/Button';

const Menu = () => {
    return (
        <>
            <Button id="home" variant="outlined" >Home</Button>
            <Button id="top-bar" >Recipes</Button>
            <Button id="top-bar"  >Recipe</Button>
            <Button id="top-bar"  >About us</Button>
            <Button id="top-bar"  >Catering</Button>
        </>
    )
};

export default Menu